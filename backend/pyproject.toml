[tool.ruff]
line-length = 88
target-version = "py311"
exclude = [
  ".venv",
  "__pycache__",
]

[tool.ruff.lint]
select = [
  "E",
  "F",
  "I",
  "B",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
